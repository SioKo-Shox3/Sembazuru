# T-001: 診断テストの環境依存で検証未完了

依存更新 e463b93 は保持する。h2 0.4.16 と webbrowser 1.2.2 に更新し、cargo-deny、Rust 1.97.0 の fmt/clippy は成功した。
PowerShell からの workspace テストは成功したが、cmd 経由の独立再検証は 2 回 exit 101 で失敗したため、タスク全体は完了していない。

実際の失敗は `sandbox_probe_record_round_trip_uses_file_not_stdout` の sandbox.rs:5061。
固定した環境で起動する子は成功するが、比較対象を作る親の `SandboxProbeRecord::collect` が `environment text` を返す。
`sandbox_probe_record_child` の stale destination による失敗表示は別の negative test の期待結果であり、原因ではない。

値を表示しない形式プローブでは、PowerShell 経由の名前に `=` を含む環境変数は 0 件、cmd 経由はドライブ情報用の特殊な名前が 1 件だった。
`validate_environment` は `=` を含む名前を拒否する。対象テスト単体でも cmd 経由の exit 101 を確認した。

再現コマンド: `cmd.exe /d /c "rustup run 1.97.0 cargo test -p sembazuru-worker --lib sandbox::tests::sandbox_probe_record_round_trip_uses_file_not_stdout --locked -- --exact"`

証拠: `.harness/parent-env-roundtrip.log`、`.harness/root-ci-observations.md`、`.harness/runs/20260908-082159/recheck-T-001-1-4.txt`、`recheck-T-001-2-4.txt`。
再開条件: 診断テストの親側の期待値収集を別タスクで修正し、cmd と PowerShell の両方で同じ検証を通す。検証コマンドの変更や入力検査の緩和で成功扱いにしない。
`sandbox.rs` は T-001 の許可パス外なので、依存更新のタスクでは変更していない。
