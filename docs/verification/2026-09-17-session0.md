# Session 0 の起動フラグ実測 — 2026-09-17

GitHub hosted runner で Session 0 の A/B 診断を初めて実測した記録。

## 実行の同一性

- run: <https://github.com/SioKo-Shox3/Sembazuru/actions/runs/35208068643>（workflow_dispatch、ブランチ指定）
- 対象 SHA: `29a13f141f769d0ce9c8047a6d70554841e1cac1`。実行前のリモート先端と一致。
- 診断 job: `Measure worker creation flags in Session 0 / session0`。run に存在し、`contents: read`、secrets 非継承、checkout は SHA 固定・資格情報非保持。
- runner: Windows Server 2025 (10.0.26100)、`windows-2025-vs2026`。
- 診断スクリプトと fixture のハッシュを実行前に検証。
  `verified_script_sha256=F4CB2941BCC254B1665E491421518A5C5EC69EE19C15EEC338643FC224836B7D`
  `expected_executable_sha256=AE001A8A8CFDBC3EE6600403B46F9C6D12537F556789F274A037E37F92677072`
- 完了条件の充足: `markers=0x07`（ENTRY|PRE_SPAWN|SPAWN_RETURNED の完全経路）、
  `SCM status note: Running completed inside the polling interval.`（worker の前後一致）、
  job は success で cleanup 経路も送出なし。

## 結果: CREATE_NO_WINDOW は原因ではない

分類は **`NO_WINDOW_NOT_SUFFICIENT`**。両アームとも子プロセスが `0xc0000142` で終了した。

| | baseline | variant |
|---|---|---|
| CreationFlags | `0x00080404` | `0x08080404` (+ `CREATE_NO_WINDOW`) |
| Job UI | `0x000000fe` | `0x000000fe` |
| SpawnSucceeded | True | True |
| ChildExit | `0xc0000142` | `0xc0000142` |
| SpawnError / Stdout / Stderr | 空 | 空 |

事前に定めた判定規則（baseline 0142 かつ variant 0 のときだけ `NO_WINDOW_CAUSAL`）に照らし、
**製品の起動フラグに `CREATE_NO_WINDOW` を追加しない。** 追加しても直らないことが実測で示された。

## 実測から読み取れる真因

同じレコードに、子プロセスが起動に失敗する理由が出ている。

```
Station=Service-0x0-27fb60$  Desktop=Default
StationAccess=mask=0x0000006e; allowed=false; gle=5
DesktopAccess=mask=0x000000cf; allowed=false; gle=5
Broker=user=S-1-5-80-1970768882-...; integrity=12288
Action=user=S-1-5-80-1970768882-...; integrity=8192
StationDacl=... sid=S-1-5-80-1970768882-... , sid=S-1-5-32-544
DesktopDacl=... sid=S-1-5-80-1970768882-... , sid=S-1-5-32-544
```

- アクションのトークンは `CreateRestrictedToken` で作られた **制限付きトークン**。
  制限 SID は `crates/worker/src/sandbox.rs:469-487` で
  `[action_sid(アクションごとの乱数), Everyone, Authenticated Users, Users, RESTRICTED]` の5つ。
- 制限付きトークンのアクセスチェックは **2回**通る必要がある。通常の SID 列で DACL を通り、
  かつ制限 SID 列でも通ること。
- ウィンドウステーションとデスクトップの DACL は、サービス SID と `S-1-5-32-544`（Administrators）にしか
  許可を与えていない。**制限 SID 側に一致する ACE が1つもない。**
- したがって通常側が通っても制限側で必ず落ちる。実測の `allowed=false; gle=5` と整合する。

ウィンドウステーションとデスクトップを開けないプロセスは初期化に失敗し、`0xC0000142`
(`STATUS_DLL_INIT_FAILED`) で終了する。`CREATE_NO_WINDOW` はこの接続自体を止めないので効かない。

整合性ラベルの差（Action=8192 / Broker=12288）は測定値としては正しいが、**原因ではない**。
Medium 以上のプロセスが作ったオブジェクトには明示ラベルが付かず Medium 扱いになるため、
Medium のアクションに no-write-up は効かない。整合性を上げても制限側の DACL 不一致は残る。
この機構の切り分けは 2026-09-17 の Advisor 相談で指摘を受け、上記のコード位置で裏取りした。

## この実測が置き換える前提

- 「installed worker の 0xC0000142 は起動フラグで直る」という作業前提は**否定された**。
- CI の `m9_installer_acl.ps1:1332` が `--no-vfs` の plain control で失敗するのも同じ原因で、
  フック DLL は関与しない。
- T-006 で扱ったログオン単位のウィンドウステーション生成は、この問題の解の側にある。
  アクション専用のステーションとデスクトップを用意する設計が次の検討対象になる。
