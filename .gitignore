# Rust
/target/

# C++ / CMake build output (out-of-source builds only)
/hooks/build/
/hooks/build32/

# Visual Studio / MSVC artifacts outside build dirs
.vs/
*.user
*.obj
*.pdb
*.ilk
*.idb

# Scratch / local verification temp (untracked working dir)
/_vtmp/

# Editor / OS noise
*.swp
*.tmp
Thumbs.db
